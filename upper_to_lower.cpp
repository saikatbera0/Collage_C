#include<stdio.h>
char calculate(char ch){
	if((ch>='a')&&(ch<='z'))
	{
		ch=	
		printf("%d",ch);
	}
	return ch;
}
int main()
{
	char ch;
	printf("Enter the cherecter:");
	scanf("%c",&ch);
	if((ch>='a')&&(ch<='z'))
	{
		ch=	
		printf("%d",ch);
	}
	calculate(ch);
} 
